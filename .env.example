# Stemset Environment Variables
# Copy this file to .env and customize as needed

# Model Cache Directory
# Where AI models are downloaded and cached
# Default: ~/.stemset/models
# STEMSET_MODEL_CACHE_DIR=/path/to/cache

# PyTorch Device Override
# Force CPU or CUDA (auto-detected by default)
# Options: cpu, cuda, mps (for Apple Silicon)
# TORCH_DEVICE=cpu

# PyTorch Thread Limit Override
# Number of CPU threads for PyTorch (default: cpu_count / 2)
# TORCH_NUM_THREADS=4

# Log Level
# Options: DEBUG, INFO, WARNING, ERROR
# Default: INFO
# STEMSET_LOG_LEVEL=INFO
